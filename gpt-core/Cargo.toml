[package]
name = "gpt-core"
description = "microGPT forward/backward/Adam in deterministic Q32.32 fixed-point — no_std, zero dependencies"
version = "0.1.0"
authors = { workspace = true }
edition = { workspace = true }
license = { workspace = true }
repository = { workspace = true }

[dependencies]
